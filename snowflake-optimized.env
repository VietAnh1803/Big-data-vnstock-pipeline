# Optimized Snowflake Configuration for Free Tier
# Reduced sync frequency to save credits

# PostgreSQL Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=stock_db
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin

# Snowflake Configuration
SNOWFLAKE_ACCOUNT=BRWNIAD-WC21582
SNOWFLAKE_USER=BRWNIAD
SNOWFLAKE_PASSWORD=Vanh@123456
SNOWFLAKE_WAREHOUSE=COMPUTE_WH
SNOWFLAKE_DATABASE=STOCKS
SNOWFLAKE_SCHEMA=PUBLIC
SNOWFLAKE_ROLE=ACCOUNTADMIN

# Optimized Sync Intervals (to save credits)
SYNC_INTERVAL=3600  # 1 hour instead of 5 minutes (saves 12x credits)
FETCH_INTERVAL=1800  # 30 minutes instead of 5 minutes

# Data Retention Settings
DATA_RETENTION_DAYS=90  # Keep only 90 days of realtime data
HISTORICAL_DATA_RETENTION_DAYS=365  # Keep 1 year of historical data

# Batch Size Optimization
BATCH_SIZE=5000  # Larger batches, fewer operations
BATCH_TIMEOUT=30  # Longer timeout for larger batches