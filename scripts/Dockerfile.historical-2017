FROM python:3.10-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir \
    vnstock \
    pandas \
    psycopg2-binary \
    python-dotenv

# Copy script
COPY fetch_historical_from_2017.py /app/

# Run the historical data fetcher
CMD ["python", "fetch_historical_from_2017.py"]
