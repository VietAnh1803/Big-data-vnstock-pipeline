FROM python:3.10-slim

WORKDIR /app

# Install dependencies
RUN pip install --no-cache-dir \
    vnstock3 \
    psycopg2-binary \
    pandas \
    python-dotenv

# Copy scripts
COPY fetch_all_data_v2.py /app/fetch_all_data.py

CMD ["python", "/app/fetch_all_data.py"]

